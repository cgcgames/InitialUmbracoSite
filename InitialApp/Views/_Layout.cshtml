@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@{
    //var content = Model.Content as SeoComposition;
    Layout = null;
    var metaTitle = Model.Content.Name;

    //if (content != null)
    //{
    //    metaTitle = !string.IsNullOrEmpty(content.MetaTitle) ? content.MetaTitle : metaTitle;
    //}
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>@metaTitle</title>
    @*@if (content != null && !string.IsNullOrEmpty(content.MetaDescription))
        {
            <meta name="description" content="@content.MetaDescription">
        }*@
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>

<body>
    @RenderBody()
</body>

</html>
